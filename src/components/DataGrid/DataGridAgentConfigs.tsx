
export default function DataGridAgentConfigs() {
    /*
    <DataGrid
        //onCellEditStop={(selected,event)=>updateAgentConfig(selected, event)}
        rows={rows}
        columns={columns}
        pageSize={5}
        rowsPerPageOptions={[5]}
        rowHeight={150}
        headerHeight={70}
        autoHeight={true}
        experimentalFeatures={{newEditingApi: true}}
    />*/
    return (
        <div></div>

    );
}